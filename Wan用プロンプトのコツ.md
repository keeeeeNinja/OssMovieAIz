# Wan (Wan2.1) 動画生成プロンプト最適化ガイド

Civitai等のコミュニティで共有されている、Wanモデルでより高品質な動画を生成するためのプロンプト術のまとめです。

---

## 1. プロンプトの構造と詳細度
* **理想的な長さ:** 最高の成果を出すには **80〜100単語程度** の具体的で鮮明な描写が推奨されます。
* **基本構成:** `[被写体] + [シーン] + [アクション] + [カメラワーク] + [雰囲気] + [スタイル] + [照明] + [ショットサイズ]`
    の順で要素を積み上げると、AIが意図を理解しやすくなります。
* **LLMの活用:** プロンプト作成時にGemini 2.0などのLLMを使用して、詳細なキャプションを生成させてから入力する手法が非常に効果的です。

## 2. カメラワークの明示
動画特有の「動き」を制御するために、以下のキーワードを積極的に含めることが重要です。
* **カメラの動き:** `Pan Left/Right`（パン）、`Tilt Up/Down`（チルト）、`Dolly In/Out`（ドリー）、`Tracking Shot`（追従）
* **画角の指定:** `Close-up`（接写）、`Wide Shot`（引き）、`Low Angle`（ローアングル）

## 3. 照明と雰囲気の指定
映像の質感をプロレベルに引き上げるために、ライティングを詳細に記述します。
* **推奨キーワード:** * `Cinematic lighting`（映画のようなライティング）
    * `Volumetric lighting`（光の筋、チンダル現象）
    * `Soft light`（柔らかな光）
    * `Cyberpunk style`（特定のスタイル指定）

## 4. T2VとI2Vの使い分け
生成モードによってプロンプトの力加減を変えるのがコツです。
* **T2V (Text to Video):** 被写体だけでなく、背景の変化や気象条件（霧の中から現れる、雪が吹き付ける等）を盛り込むと、動きがダイナミックになります。
* **I2V (Image to Video):** 元画像に視覚情報があるため、プロンプトは**簡潔**にします。「何がどう動くか」というアクション部分に記述を集中させてください。

## 5. 高度なテクニックとトラブルシューティング
* **トリガーワード:** 特定のLoRAを使用している場合は、学習されたスタイルを呼び出すためのトリガーワードを忘れずに入力します。
* **生成の安定化:** 動画にノイズや激しい揺れ（Shaking）が発生する場合は、SLG（Skip Layer Guidance）の設定を調整するか、プロンプト内の動きの指定を少し弱めることで改善する場合があります。